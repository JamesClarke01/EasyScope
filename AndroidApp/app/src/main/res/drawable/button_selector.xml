<?xml version="1.0" encoding="utf-8"?>
<!-- button_background.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@color/btnPressed" />
    <!-- Focused state -->
    <item android:state_focused="true" android:drawable="@color/btnPressed" />
    <!-- Default state -->
    <item android:drawable="@color/background" />
</selector>